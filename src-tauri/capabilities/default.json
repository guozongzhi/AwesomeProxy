{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the AwesomeProxy settings window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
